linters:
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - errcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck

run:
  tests: false

issues:
  exclude-use-default: false
  exclude-dirs:
    - vendor